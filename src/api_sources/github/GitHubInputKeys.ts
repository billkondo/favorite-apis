import GITHUB_KEY from './GitHubKey';

export const GITHUB_NAME = `${GITHUB_KEY}_name`;
export const GITHUB_SORTBY = `${GITHUB_KEY}_sortBy`;

const GitHubInputKeys = [GITHUB_NAME, GITHUB_SORTBY];

export default GitHubInputKeys;
